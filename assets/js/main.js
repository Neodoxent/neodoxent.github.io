(function(){"use strict";const M=window.NEODOXENT_MODULES,S=window.NEODOXENT_STATE;function el(q){return document.querySelector(q)}function renderSegments(module,node){if(!module.segments)return;S.enter(module.id);let wrap=node.querySelector('.segments');if(!wrap){wrap=document.createElement('div');wrap.className='segments';node.appendChild(wrap)}wrap.innerHTML='';module.segments.forEach(seg=>{const b=document.createElement('button');b.textContent=seg.title;const done=S.get().segments[module.id]?.[seg.id];b.disabled=done;b.onclick=()=>{S.completeSegment(module.id,seg.id);render()};wrap.appendChild(b)})}
function render(){const st=S.get();M.forEach(m=>{const node=el(`[data-module="${m.id}"]`);if(!node)return;renderSegments(m,node);const btn=node.querySelector(`[data-accept]`);if(btn){btn.disabled=!S.canAccept(m.id)||st.accepted.includes(m.id)}})}document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-accept]').forEach(b=>b.onclick=()=>{S.accept(b.dataset.accept);render()});render();setInterval(render,1000)});
})();